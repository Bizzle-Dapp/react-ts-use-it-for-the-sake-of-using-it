// Enum to provide our routing strings. What we see in the URL atop the browser.
export enum pageTypes {
    landingPage = '/',
    timeOnApp = '/timeOnApp',
    detailsForm = '/detailsForm',
    inputTest = '/inputTest'
  }